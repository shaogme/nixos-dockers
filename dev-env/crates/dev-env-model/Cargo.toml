[package]
name = "dev-env-model"
version = "0.1.0"
edition = "2021"
rust-version = "1.74"
description = "Pure data model and static validation for the dev-env DSL"
license = "MIT"

[dependencies]
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
serde_json = "1.0"
toml = "0.8"
