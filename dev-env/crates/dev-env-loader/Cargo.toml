[package]
name = "dev-env-loader"
version = "0.1.0"
edition = "2021"
rust-version = "1.74"
description = "Profile graph loader and strict merger for the dev-env DSL"
license = "MIT"

[dependencies]
dev-env-model = { path = "../dev-env-model" }
serde = { version = "1.0", features = ["derive"] }
toml = "0.8"
